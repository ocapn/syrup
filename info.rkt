#lang info

(define deps '("base"))
(define build-deps '())
(define pkg-desc
  "Lightweight binary serialization based on Preserves, with some extra calories")
(define version "0.0")
(define pkg-authors '("cwebber"))
#;(define scribblings '(("scribblings/syrup.scrbl")))
